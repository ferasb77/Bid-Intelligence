# ==============================================================================
# Bid Intelligence — Environment Configuration Template
# Copy this file to .env and configure local keys.
# NEVER commit the actual .env file or real credentials to version control.
# ==============================================================================

# Anthropic API Configuration
ANTHROPIC_API_KEY=sk-ant-paste-your-key-here

# Required ONLY if ANTHROPIC_API_KEY above is a workspace-scoped key
# (created inside a specific Anthropic Console workspace, not an
# account-level/legacy key). A workspace-scoped key rejects every
# request with 400 invalid_request_error: "This API key is not scoped
# to a workspace, so this request must include the anthropic-workspace-
# id header" until this is set. config.get_anthropic_client() is the
# ONLY place in this codebase that reads it and attaches the header --
# every Anthropic API call in the app (analyst.py, extractor.py,
# fast_analysis.py) must go through that function, never construct
# anthropic.Anthropic(...) directly, or this setting has no effect for
# that call site. Leave blank for an account-level key (no header sent).
ANTHROPIC_WORKSPACE_ID=
ANTHROPIC_CUSTOM_HEADERS=

# Live Supabase Database Configuration
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_KEY=your-supabase-service-role-key-here

# Supabase Auth (Phase 8 remediation package 2) -- the anon/publishable
# key, NOT the service-role key. Safe to embed in a browser-facing client
# by Supabase's own design; used only by auth_client.py for sign-in/
# sign-out/session handling, never for ordinary database access.
SUPABASE_ANON_KEY=your-supabase-anon-or-publishable-key-here

# Phase 8 remediation package 3 -- this deployment's own public base URL,
# used as email_redirect_to when a user requests a sign-in link from the
# login gate. Must exactly match a URL already added to Supabase Auth's
# Redirect URLs allowlist, or the magic-link callback will fail. Update
# this whenever the deployment target changes (e.g. staging -> production).
APP_BASE_URL=https://your-deployed-app-url.example.com/

# Voyage AI Embeddings Configuration (Optional)
VOYAGE_API_KEY=
